name: Test Atmos Terraform Apply - Actions Disabled
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with actions disabled for component
        id: action
        uses: .
        with:
          component: test-component
          stack: test-stack
          sha: ${{ github.sha }}
          skip-checkout: "true"
          atmos-config-path: >-
            ${{ github.workspace }}/tests/fixtures/atmos-config
          plan-storage: "false"
          debug: "false"

      - name: Assert status output is empty when actions disabled
        env:
          STATUS: ${{ steps.action.outputs.status }}
        run: |
          echo "status: '$STATUS'"
          # When github-actions-enabled is false, the apply step is skipped
          # so status output should be empty
          if [ -n "$STATUS" ] && [ "$STATUS" != "" ]; then
            echo "FAIL: expected empty status when actions disabled, got: '$STATUS'"
            exit 1
          fi
          echo "PASS: status is empty as expected when actions are disabled"
